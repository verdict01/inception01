# V5.10 Inception - ModernBERT Embeddings
# Railway CPU deployment (always-on, no cold starts)
# NOTE: torch installed separately as CPU-only in Dockerfile

sentence-transformers>=3.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
prometheus-client>=0.21.0
nltk>=3.9.0
# torch installed separately in Dockerfile (CPU-only to reduce image size)
transformers>=4.46.0
